{
  "name": "ai-road-condition-detector",
  "version": "1.0.0",
  "description": "AI-powered road condition detection and reporting system",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:ai": "cd ai-service && python app.py",
    "install:all": "cd server && npm install && cd ../client && npm install && cd ../ai-service && pip install -r requirements.txt"
  }
}
